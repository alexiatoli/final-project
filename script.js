// Placeholder for future interactivity
document.querySelectorAll('.job').forEach(job => {
  job.addEventListener('click', () => {
    alert(`Job: ${job.textContent}\nMore info coming soon!`);
  });
});
